package SeleniumAutomation.Enums;

//Enum for storing UI credentials
public enum ECredentials {
    ADMIN_USER,
    ADMIN_USER_PASS,
    DEV_USER,
    DEV_USER_PASS,
    TESTER_USER,
    TESTER_USER_PASS
}
